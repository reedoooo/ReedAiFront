export default {
  styleOverrides: {
    // the slot name defined in the `slot` and `overridesResolver` parameters
    // of the `styled` API
    root: {
      backgroundColor: '#121212',
      color: '#fff',
      fontWeight: 600,
      fontSize: '1rem',
    },
    value: {
      color: '#fff',
    },
    unit: {
      color: '#888',
    },
  },
};
